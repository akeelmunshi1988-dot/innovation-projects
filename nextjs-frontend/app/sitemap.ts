import type { MetadataRoute } from 'next';import { getCatalog,siteUrl } from '@/lib/api';export default async function sitemap():Promise<MetadataRoute.Sitemap>{const catalog=await getCatalog({limit:500});const now=new Date();return [{url:siteUrl,lastModified:now,changeFrequency:'weekly',priority:1},{url:`${siteUrl}/catalog`,lastModified:now,changeFrequency:'daily',priority:.9},{url:`${siteUrl}/about`,lastModified:now,changeFrequency:'monthly',priority:.6},{url:`${siteUrl}/custom-rug-request`,lastModified:now,changeFrequency:'monthly',priority:.7},...catalog.items.map(r=>({url:`${siteUrl}/catalog/${r.slug}`,lastModified:now,changeFrequency:'weekly' as const,priority:.8}))]}
